<!--pages/home/home.wxml-->
<view>
  <!--顶部-->
  <view>
    <fui-row marginTop="30rpx" margin-bottom="24rpx">
      <fui-col offset="1" span="{{5}}">
        <fui-avatar size="large" src="{{userInfo.HeadeImg}}"></fui-avatar>
      </fui-col>
      <fui-col span="{{17}}">
        <view>
          <fui-text text="{{userInfo.Name}}" size="{{48}}" fontWeight="500"></fui-text>
        </view>
        <view class="view-text">
          <fui-text text="注册时间：{{userInfo.registerTime}}" size="{{24}}" color="#333333" ></fui-text>
        </view>
      </fui-col>
    </fui-row>
  </view>
  <loading wx:if="{{pageStatus === -1}}"  type="col" />
  <empty wx:elif="{{pageStatus === 0}}" type="0" marginTop="200"  descr="暂无数据"> 
    <ui-button bindclick="bindRight" text="去创建一个组" size="{{28}}" width="336rpx" height="84rpx" radius="100rpx" background="#fff" margin="64rpx 0" borderColor="#12c194" color="#12c194"/>
  </empty>
  <empty wx:elif="{{pageStatus === 3}}" type="3" marginTop="200"  descr="网络信号找不着路了！"/>
  <block wx:else>
    <view class="view-margin">
      <swiper circular indicator-dots="{{true}}" class="swiper-main" indicator-active-color="#fcce1c"> 
        <swiper-item wx:for="{{groups}}" wx:key="index" class="swiper-item">
          <!-- <view class="fui-banner__item fui-item__scale" style="background:{{item.background}}">我的爱车</view> -->
          <view wx:if="{{item.type === 'info'}}" class="card">
            <view class="card-hd" >
              <image class="card-hd-l" src="../../static/img/home/{{item.level === '1' ? 'lead' : 'staff'}}.png"  mode="widthFix"/>
              <view class="card-hd-t">{{item.user_name}}</view>
              <image wx:if="{{item.level === '1'}}" class="card-hd-r"  src="../../static/img/home/icon-right.png"  mode="widthFix"/>
            </view>
            <view class="group-name">{{item.group_name}}</view>
            <view class="group-desc">{{item.group_desc}}</view>
          </view>

        </swiper-item>
      </swiper>
    </view>
  </block>
  <!--中部-->
  

  <!--底部-->
  <!-- <view class="view-margin">
    <fui-row margin-bottom="24rpx">
      <fui-col span="{{11}}" pushLeft="1">
        <view class="fui-col__style">
          <view>
            <view>
              <view class="fui-icon__viewstyle">
                <fui-icon name="edit-fill" color="#39b778" size="{{80}}"></fui-icon>
              </view>
              <view class="fui-icon__viewstyle">
                <fui-text text="创建" size="{{35}}"></fui-text>
                <fui-icon name="arrowright" size="{{38}}" class="fui-icon__style"></fui-icon>
              </view>
            </view>

          </view>
        </view>
      </fui-col>
      <fui-col span=" {{11}}" pushLeft="1">
        <view class="fui-col__style">
          <view class="fui-col__style">
            <view>
              <view>
                <view class="fui-icon__viewstyle">
                  <fui-icon name="classify-fill" color="#39b778" size="{{80}}"></fui-icon>
                </view>
                <view class="fui-icon__viewstyle">
                  <fui-text text="更多" size="{{35}}"></fui-text>
                  <fui-icon name="arrowright" size="{{38}}" class="fui-icon__style"></fui-icon>
                </view>
              </view>
            </view>
          </view>
        </view>
      </fui-col>
    </fui-row>

  </view> -->
</view>